# 行列関数

スカラー関数 `f(x)` を行列へ拡張したものを `f(A)` と考えます。

`A=PΛP^{-1}` と対角化できるなら

\[
f(A)=Pf(\Lambda)P^{-1}
\]

と定義できます。`f(Λ)` は各固有値に `f` を適用した対角行列です。

## 例

- `A^k`
- `e^A`
- `sin A`
- `log A`

などです。

## 本質

固有値分解が有用なのは、行列関数を「各固有方向に対するスカラー関数」へ還元できるからです。
