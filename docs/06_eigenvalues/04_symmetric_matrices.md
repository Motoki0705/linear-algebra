# 対称行列

実行列 `A` が

\[
A^\top=A
\]

を満たすとき対称行列と呼びます。

対称行列は非常に特別で、

- 固有値がすべて実数
- 異なる固有値の固有ベクトルは直交
- 正規直交固有基底を持つ

という性質があります。

したがって

\[
A=Q\Lambda Q^\top
\]

と分解できます。

## なぜ SVD の前に重要か

一般行列 `A` に対して `A^TA` と `AA^T` は必ず対称行列です。SVD はこれらの固有値分解から構成できます。

つまり対称行列の理論は SVD の直接の土台です。
