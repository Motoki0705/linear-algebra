# 特性多項式

固有値は

\[
Av=\lambda v
\]

から

\[
(A-\lambda I)v=0
\]

を満たす非零 `v` が存在する値です。

非零解が存在するには `A-λI` が非可逆である必要があるため

\[
\boxed{\det(A-\lambda I)=0}
\]

となります。

左辺を特性多項式と呼びます。

## 意味

この導出で重要なのは公式を覚えることではなく、「固有値とは `A-λI` がある方向を零へ潰すようになる `λ`」という見方です。
