# 固有値と固有ベクトル

非零ベクトル `v` が

\[
Av=\lambda v
\]

を満たすとき、`v` を固有ベクトル、`λ` を固有値と呼びます。

## 幾何学的意味

一般のベクトルは `A` によって方向も大きさも変わります。しかし固有ベクトルは方向が保存され、伸縮率だけが `λ` になります。

したがって固有ベクトルは、線形変換にとって自然な座標軸です。

## 注意

固有値問題は原則として同じ空間への写像 `A:R^n→R^n` に対して考えます。一方 SVD は長方形行列にも定義でき、入力側と出力側で別々の方向を使います。
