# 正規直交基底

基底 `q_1,...,q_n` が

\[
q_i^\top q_j=\delta_{ij}
\]

を満たすとき正規直交基底と呼びます。

列に並べた行列 `Q` は

\[
Q^\top Q=I
\]

を満たします。

正方行列なら

\[
Q^{-1}=Q^\top
\]

です。

## 幾何学的意味

直交行列は長さと角度を保存します。

\[
\|Qx\|_2=\|x\|_2
\]

したがって回転や鏡映を表します。

SVD の `U,V` が直交行列であるため、SVD では本質的な伸縮量が `Σ` に完全に分離されます。
