# 行列

行列は数を長方形に並べたものですが、線形代数では **線形写像の座標表現** として理解することが最重要です。

\[
A\in\mathbb{R}^{m\times n}
\]

は `R^n` の入力を `R^m` の出力へ写します。

\[
x\mapsto Ax
\]

## 3つの見方

### 行による見方

各出力成分は、入力との内積です。

### 列による見方

`Ax` は `A` の列の一次結合です。

### 写像による見方

空間全体を伸縮・せん断・回転・射影します。

この3つを行き来できることが重要です。
