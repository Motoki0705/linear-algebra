# 可逆性

正方行列 `A∈R^{n×n}` について、次はすべて同値です。

- `A` は可逆
- `Ax=b` は任意の `b` に対して一意解を持つ
- `ker(A)={0}`
- `rank(A)=n`
- 列ベクトルが一次独立
- 列ベクトルが `R^n` を張る
- `det(A)≠0`
- 0 は固有値でない
- すべての特異値が正

これらは別々の暗記事項ではありません。

本質はただ一つ、**A がどの入力方向も潰していない** という事実を、方程式・空間・行列式・固有値・SVD という異なる言語で表現しているだけです。
