# 数値安定性

数値安定性は、丸め誤差を含む計算がどれだけ信頼できるかを扱います。

## conditioning と stability

- conditioning: 問題そのものが入力誤差にどれだけ敏感か
- stability: アルゴリズムが追加誤差をどれだけ生むか

この二つを混同してはいけません。

例えば悪条件な `A` に対して正規方程式を使うと

\[
\kappa(A^TA)=\kappa(A)^2
\]

となり問題をさらに厳しくします。そのため最小二乗では QR や SVD が好まれる場合があります。
